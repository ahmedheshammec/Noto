→ to enter vim mode on a file type: 

```bash
vim .profile
```

→ type `i` to enter insert mode

→ To Save & Exit Press `Esc` then type `:wq` and hit Enter

→ To Just save (stay in vim): Press `Esc` then type `:w` and hit Enter

→ To Exit without saving: Press `Esc` then type `:q!` and hit Enter

